import Header from '../Header';
import Playground from '../Playground';

import './styles.scss';

const Layout = () => {
  return (
    <div className="layout">
      <Header />
      <Playground />
    </div>
  );
};

export default Layout;
